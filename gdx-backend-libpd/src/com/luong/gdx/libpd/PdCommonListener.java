package com.luong.gdx.libpd;

/**
 * A dummy interface so we can have a common addListener() and removeListener methods.<br>
 * <br>
 * <b>All PdListeners most implement this interface too.</b>
 */
public interface PdCommonListener {

	}
